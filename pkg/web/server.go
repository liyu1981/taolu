package web

const (
	// serverName and serverVersion mirror the MCP server identity so the status
	// view reports the same build the user is talking to.
	serverName    = "taolu"
	serverVersion = "0.1.0"
)
